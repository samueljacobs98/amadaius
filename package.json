{
  "name": "amadaius-workspace",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "lint": "turbo lint",
    "build": "turbo build",
    "test": "turbo test",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
