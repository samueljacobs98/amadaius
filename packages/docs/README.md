# @docs

This package contains the documentation content for the Amadaius project.

## Structure

- `content/docs/` - Contains the MDX documentation files
- `package.json` - Package configuration with exports for the content

## Adding New Documentation

Add new MDX files to the `content/docs/` directory. The content will be automatically available to any application that depends on this package.
